; Production PHP hardening (Hostinger reads .user.ini per-directory for the whole
; docroot, including api/). Never render errors — including fatals/parse errors
; outside the app's try/catch — to the browser; log them server-side instead.
display_errors = 0
display_startup_errors = 0
log_errors = 1
